# 构建上下文：项目根目录（Docker 在此查找 .dockerignore）
# 忽略一切与后端镜像无关的内容，缩小上下文、加速构建
**/node_modules
**/dist
**/dist-electron
**/release
**/build
**/.cache
**/*.log
**/.DS_Store
**/Thumbs.db

# 前端/Electron 相关（不打进后端镜像）
# 注意：site 不排除，nginx 镜像构建需要 VitePress 源码
electron
scripts
src

# deploy/ 包含 Compose 文件、Dockerfile 和 Nginx 配置，必须保留在构建上下文中
# 其中的 .env 仍由下方规则排除

# 环境变量（运行期通过 .env / compose 注入，不烤进镜像）
**/.env
**/.env.*
!**/.env.production.example

# Git / CI / 编辑器
.git
.github
.vscode
.idea
**/*.tsbuildinfo
