# 客户端环境变量示例
# Vite 会根据运行模式自动加载对应的 .env.<mode> 文件：
#   - npm run dev          → 加载 .env.development（本地后端）
#   - npm run build        → 加载 .env.production（线上后端）
#   - npm run dist         → 加载 .env.production（线上后端，产出安装包）
#
# 也可通过显式指定 mode 覆盖：
#   npm run build:local    → vite build --mode development（打一个连本地后端的安装包，用于内网测试）
#   npm run build:prod     → vite build --mode production（打线上安装包）

# 客户端连接的后端 API 基础地址（不要加末尾斜杠）
VITE_API_BASE_URL=http://localhost:4178