node_modules/
dist/
dist-electron/
*.log
.DS_Store

# 打包/构建产物
release/
site/.vitepress/dist
site/.vitepress/cache
server/dist

# 安装包分发目录（只保留 .gitkeep 占位，二进制包不提交）
deploy/releases/*
!deploy/releases/.gitkeep

# 环境变量：.env / .env.local 含本地密钥不提交；.env.development/.env.production 只含 API 地址，可提交
.env
.env.local
.env.*.local
!.env.example
!.env.development
!.env.production
!.env.production.example

# 系统/编辑器
*.local
Thumbs.db

# TypeScript 增量构建缓存
*.tsbuildinfo